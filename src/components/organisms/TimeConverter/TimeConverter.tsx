import React from 'react';

interface ITimeConverterProperties {}

const TimeConverter: React.FC<ITimeConverterProperties> = ({  }) => {

    return (
        <h1>
            TimeConverter
        </h1>
    );
};

export default TimeConverter;
